<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Technische Störung</title>
    <style>
        :root {
            color-scheme: light;
            --background: #f5f1ee;
            --surface: #fffdfc;
            --text: #1f2933;
            --muted: #52606d;
            --border: #d9c5ba;
            --accent: #b24419;
        }

        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            min-height: 100vh;
            font-family: Georgia, "Times New Roman", serif;
            background:
                radial-gradient(circle at top left, rgba(178, 68, 25, 0.14), transparent 28rem),
                linear-gradient(180deg, #fdfaf8 0%, var(--background) 100%);
            color: var(--text);
            display: grid;
            place-items: center;
            padding: 1.5rem;
        }

        main {
            width: min(100%, 46rem);
            background: rgba(255, 253, 252, 0.96);
            border: 1px solid var(--border);
            border-radius: 1.25rem;
            padding: 2.5rem 2rem;
            box-shadow: 0 1rem 3rem rgba(31, 41, 51, 0.08);
        }

        .eyebrow {
            margin: 0 0 0.75rem;
            font-size: 0.85rem;
            font-weight: 700;
            letter-spacing: 0.16em;
            text-transform: uppercase;
            color: var(--accent);
        }

        h1 {
            margin: 0 0 1rem;
            font-size: clamp(2rem, 4vw, 3.1rem);
            line-height: 1.05;
        }

        .content {
            margin-top: 1.5rem;
            padding-top: 1.25rem;
            border-top: 1px solid var(--border);
            color: var(--muted);
            font-size: 1.06rem;
            line-height: 1.7;
        }
    </style>
</head>
<body>
<main>
    <p class="eyebrow">Derzeit gestört</p>
    <h1>{{ pimcore_input("headline", {"placeholder": "Technische Störung"}) }}</h1>
    <div class="content">{{ pimcore_wysiwyg("content", {"height": 260}) }}</div>
</main>
</body>
</html>
